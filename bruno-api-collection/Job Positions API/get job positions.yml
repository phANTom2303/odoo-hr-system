info:
  name: get job positions
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5001/api/job-positions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
