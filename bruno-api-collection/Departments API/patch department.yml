info:
  name: patch department
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:5001/api/departments/1
  body:
    type: json
    data: |2
          {
            "name": "Engineering v3"
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
