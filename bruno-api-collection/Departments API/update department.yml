info:
  name: update department
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:5001/api/departments/1
  body:
    type: json
    data: |2
          {
            "name": "Engineering v2"
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
