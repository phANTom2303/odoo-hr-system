info:
  name: delete pay run
  type: http
  seq: 9

http:
  method: DELETE
  url: http://localhost:5001/api/pay-runs/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |2
    Deletes a draft pay run and its employee selection.

    Roles: admin, hr_payroll_manager

    Draft only: 409 for a computed, validated or paid run — finalised
    payroll batches are preserved as history.
    404 when the run does not exist.
