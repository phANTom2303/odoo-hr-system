info:
  name: create schedule
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5001/api/schedules
  body:
    type: json
    data: |2
          {
            "name": "Standard 90 hours",
            "lines": []
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
