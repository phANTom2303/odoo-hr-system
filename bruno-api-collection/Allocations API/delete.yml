info:
  name: delete allocation (draft only)
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:5001/api/allocations/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
