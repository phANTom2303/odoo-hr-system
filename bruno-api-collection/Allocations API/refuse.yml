info:
  name: refuse allocation
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:5001/api/allocations/1/refuse
  auth: inherit

headers:
  - name: Content-Type
    value: application/json

body:
  mode: json
  json: '{}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
