info:
  name: get rules by structure
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:5001/api/salary-structures/1/rules
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
