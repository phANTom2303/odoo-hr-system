info:
  name: create fixed rule
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5001/api/salary-structures/1/rules
  body:
    type: json
    data: |2
          {
            "code": "BASIC",
            "name": "Basic Salary",
            "category": "Basic",
            "sequence": 10,
            "rule_type": "fixed",
            "fixed_amount": 5000.00
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
