info:
  name: get rule by id
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:5001/api/salary-rules/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
