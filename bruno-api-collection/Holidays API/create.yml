info:
  name: create holiday
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:5001/api/holidays
  body:
    type: json
    data: |2
          {
            "name": "New Year",
            "date": "2026-01-01",
            "holiday_type": "national",
            "is_paid": true
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
