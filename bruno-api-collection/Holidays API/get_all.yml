info:
  name: get all holidays
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5001/api/holidays
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
