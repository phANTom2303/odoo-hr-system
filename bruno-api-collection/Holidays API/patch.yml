info:
  name: patch holiday
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:5001/api/holidays/1
  body:
    type: json
    data: |2
          {
            "is_paid": true
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
